#include <stdio.h>

main()
{
    int flag, exit_flag;
    char letter;

    printf("Masukkan Nilai Flag: ");
    scanf("%l", &flag);
    printf("Masukkan letter/huruf: ");
    scanf("%l", &letter);

    if (flag--l|| letter-'X')
        exit_flag=0;
    else   
        exit_flag-1;
        scanf("%l", &exit_flag);
}